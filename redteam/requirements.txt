anthropic>=0.39
requests>=2.32
pydantic>=2.9
python-dotenv>=1.0
psycopg2-binary>=2.9
langgraph>=0.2.34
pyyaml>=6.0
langfuse>=3
# Parses OpenEMR's OAuth2 authorization-server login + scope-consent HTML forms (openemr_adapter.py
# drives the same popup flow the chat widget uses) -- regex was not reliable enough for the
# consent screen's many dynamically-named checkboxes.
beautifulsoup4>=4.12
# Postgres-backed checkpointer for the human-approval LangGraph interrupt (app/human_gate.py) --
# proven live to survive a real process restart before being wired into the real graph, see
# Gauntlet/STATUS.md. Needs psycopg[binary] specifically (not psycopg2) -- this checkpointer
# package is built on psycopg 3.
langgraph-checkpoint-postgres>=3.0
psycopg[binary]>=3.1
