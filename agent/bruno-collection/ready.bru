meta {
  name: Ready
  type: http
  seq: 2
}

get {
  url: {{base_url}}/ready
  body: none
  auth: none
}

docs {
  Readiness probe (W2_ARCHITECTURE.md Section 9) -- reports "ok" | "degraded" | "down" plus a
  per-dependency breakdown (core_fhir_chat, document_storage, vector_index, voyage_api). Only
  core_fhir_chat failing reports "down"; the other three degrade gracefully.
}
